# QUICKSTART

1. Set environment variables (BOT_TOKEN, WEBHOOK_SECRET, OWNER_ID)
2. Deploy to Deno Deploy or run locally with deno run --allow-net --allow-env main.ts
3. Register webhook: deno run --allow-net --allow-env scripts/setup-webhook.ts
